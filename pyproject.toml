[project]
name = "honcho_shared_memory"
version = "0.0.6"
description = "Honcho Shared Memory plugin for Agent Zero"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "honcho-ai>=2.0.0",
]
